<Map>
    <IdentificationNumber>{ "required": true, "unique": true, "fixed": true }</IdentificationNumber>
    <Type>{ "vocabulary": "MapType" }</Type>
    <ManagingInstitution>{ "instance": "Group" }</ManagingInstitution>
    <!--
    Annette: In my opinion this might be the same as what is called Originator of Reference in the
    original FORTH schemas, although I am not completely sure; perhaps you can ask Martin.
    I think usually the institution who helds the original object is also the institution who publishes
    (meta)data about the object. Of course the name of the institution who helds the object must be
    mentioned in each record. So this has to be added to the other schemas.
    -->
    <!--todo: find a solution for groupSpecific -->
    <CollectionName>{ "vocabulary":"MapCollection", "groupSpecific":true }</CollectionName>
    <Title>{ "required": true, "summaryField": "Title" }</Title>
    <ParallelTitle>{ "multiple":true }</ParallelTitle>
    <Subject>{ "multiple": true }
        <SubjectTag>{ "vocabulary": "MapSubjectTag", "multiple": true }</SubjectTag>
        <SubjectEntity>
            <Entity>{ "instance": [ "Location" ] }</Entity>
            <Relation>{ "vocabulary": "MapReferenceToRelation" }</Relation>
        </SubjectEntity>
        <Scale/>
        <Orientation/>
        <Projection/>
        <Coordinates/>
        <Note>{ "paragraph":true }</Note>
    </Subject>

    <!--todo: perhaps this should be "collection" or at least a vocabulary-->
    <Series>
        <SeriesTitle/>
        <NumberWithinSeries/>
    </Series>
    <DigitalObject>{ "multiple": true }
        <File>{ "instance": "MediaMetadata }</File>
        <CreationDate>{ "validator": "date" }</CreationDate>
        <TechnicalDescription>
            <PixelsX>{ "validator": "count" }</PixelsX>
            <PixelsY>{ "validator": "count" }</PixelsY>
            <ResolutionPPI>{ "validator": "count" }</ResolutionPPI>
            <ColorDepthBits>{ "validator": "count" }</ColorDepthBits>
            <ColorModel>{ "??": "??" }</ColorModel>
            <ExifData>{ "paragraph": true }</ExifData>
        </TechnicalDescription>
        <Note>{ "paragraph":true }</Note>
    </DigitalObject>
    <ComposedOf>{ "multiple": true }
        <Book>{ "instance": ["Book"] }</Book>
        <Relation>{ "vocabulary": "BookIncludedInRelation" }</Relation>
    </ComposedOf>

    <Description>{ "paragraph":true }</Description>

    <Note>{ "paragraph": true }</Note>

    <!--todo: the spreadsheet suggests that this is a link to an image, but the name doesn't-->
    <ScanNumber/>

    <PhysicalObject>
        <Condition>{ "vocabulary": "PhysicalMapCondition" }</Condition>
        <Edition/>
        <!--todo: find a solution for groupSpecific -->
        <StorageLocation>{ "vocabulary": MapStorageLocation, "groupSpecific": true }</StorageLocation>
        <!--todo: printed or manuscript?-->
        <Material/>
        <!--todo: physical or technical?-->
        <Characteristics/>
        <!--todo: what units?-->
        <Size/>
        <!--todo: unclear-->
        <Scope/>
        <!--todo: Material, physical/technical characteristics, size and scope: We will ask Jos which types of fields these are.-->
        <StorageLocation>{ "vocabulary": MapStorageLocation, "groupSpecific": true }</StorageLocation>
    </PhysicalObject>

    <CreationEvent>
        <!--ask: what if the creation date is not known but is a date range?-->
        <CreationDate>{ "validator": "date" }</CreationDate>
        <CreatorInRole>{ "multiple : true" }
            <Role>{ "vocabulary": "MapCreationRole" }</Role>
            <Creator>{ "instance": [ "Person", "Group" ] }</Creator>
        </CreatorInRole>
        <CreationPlace>{ "instance": "Location" }</CreationPlace>
    </CreationEvent>

    <AcquisitionEvent>
        <AcquisitionType>{ "vocabulary": "AcquisitionTypes" }</AcquisitionType>
        <AcquisitionSource>{ "instance": [ "Person", "Group" ] }</AcquisitionSource>
        <AcquisitionDate>{ "validator": "date" }</AcquisitionDate>
    </AcquisitionEvent>

    <Literature>{ "paragraph":true }</Literature>

</Map>
